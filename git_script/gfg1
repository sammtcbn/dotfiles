#!/bin/bash
# https://github.com/sammtcbn/dotfiles
# Written by sammtcbn 2018.1.9

git config --global user.name "Sam35.Lin" || exit 1
git config --global user.email "sam35.lin@advantech.com.tw" || exit 1
cat ~/.gitconfig || exit 1
